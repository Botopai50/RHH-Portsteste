set -e

source /root/buildtools/harbourmasters/lib_build.sh

export REF="${REF:-9.2.3-celshade0.7}"

project_clone https://github.com/roborich/Shipwright.git

build_sdl2
build_sdl2_net

build_libzip
build_json
build_bzip2
build_tinyxml2
build_opus
build_opusfile
build_fmt
build_spdlog -DSPDLOG_FMT_EXTERNAL=ON

project_configure_and_build GenerateSohOtr \
    -DCMAKE_PREFIX_PATH=/usr/local \
    -DUSE_OPENGLES=1 \
    -DBUILD_REMOTE_CONTROL=1 \
    -DCMAKE_CXX_STANDARD=20 \
    -DCMAKE_CXX_STANDARD_REQUIRED=ON \
    -DCMAKE_CXX_FLAGS="-DFMT_CONSTEVAL=constexpr" \
    -DCMAKE_BUILD_TYPE=${BUILD_TYPE:-Release}

stage_libs soh/soh.elf \
    libz.so.1 \
    libogg.so.0 \
    libvorbis.so.0 \
    libvorbisenc.so.2 \
    libvorbisfile.so.3 \
    libpng16.so.16 \
    libbz2.so.1 \
    libzip.so.5 \
    libtinyxml2.so.10 \
    libopus.so.0 \
    libopusfile.so.0 \
    libSDL2_net-2.0.so.0
